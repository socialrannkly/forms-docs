---
title: "Department NPS Breakdown"
api: "GET /external-api/v1/feedback/department-nps"
description: "Get NPS score calculated per department linked to NPS-type form questions."
---

This endpoint calculates Net Promoter Score (NPS) for each department by looking at NPS-type questions that have a `departmentId` configured. Each NPS answer is classified as promoter (9–10), passive (7–8), or detractor (0–6), grouped by the linked department.

### NPS Formula

```
NPS = % Promoters − % Detractors
```

NPS ranges from **-100** (all detractors) to **+100** (all promoters).

## Request

### Headers

| Header | Value | Required |
|---|---|---|
| `X-API-Key` | Your API key (`rfk_...`) | Yes |
| `X-Team-Id` | Numeric team ID (required for global API keys) | Conditional |

### Query Parameters

<ParamField query="startDate" type="string" required>
  Start of the date range. Must be a valid ISO 8601 datetime string.

  **Example:** `2026-01-01T00:00:00.000Z`
</ParamField>

<ParamField query="endDate" type="string" required>
  End of the date range. Must be a valid ISO 8601 datetime string. Must be after `startDate`.

  **Example:** `2026-02-19T23:59:59.999Z`
</ParamField>

<ParamField query="formIds" type="string">
  Comma-separated list of form IDs (MongoDB ObjectIds) to filter by.

  **Example:** `665a1b2c3d4e5f6a7b8c9d0e,665a1b2c3d4e5f6a7b8c9d0f`
</ParamField>

<ParamField query="locationIds" type="string">
  Comma-separated list of location IDs (numeric) to filter by.

  **Example:** `1561,1562`
</ParamField>

### Example Request

<Tabs>
  <Tab title="cURL">
    ```bash
    curl -X GET "https://forms-api.rannkly.com/external-api/v1/feedback/department-nps?startDate=2026-01-14T00:00:00Z&endDate=2026-02-12T23:59:59Z&locationIds=1561" \
      -H "X-API-Key: rfk_your_api_key"
    ```
  </Tab>
  <Tab title="JavaScript">
    ```javascript
    const response = await fetch(
      "https://forms-api.rannkly.com/external-api/v1/feedback/department-nps?" +
      new URLSearchParams({
        startDate: "2026-01-14T00:00:00Z",
        endDate: "2026-02-12T23:59:59Z",
        locationIds: "1561"
      }),
      { headers: { "X-API-Key": process.env.RANNKLY_API_KEY } }
    );
    const { data } = await response.json();
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import requests

    response = requests.get(
        "https://forms-api.rannkly.com/external-api/v1/feedback/department-nps",
        headers={"X-API-Key": "rfk_your_api_key"},
        params={
            "startDate": "2026-01-14T00:00:00Z",
            "endDate": "2026-02-12T23:59:59Z",
            "locationIds": "1561"
        }
    )
    data = response.json()
    ```
  </Tab>
</Tabs>

## Response

<ResponseField name="success" type="boolean">
  `true` if the request was successful.
</ResponseField>

<ResponseField name="message" type="string">
  `"Department NPS breakdown retrieved successfully"`
</ResponseField>

<ResponseField name="data" type="object">
  <Expandable title="Data Properties">
    <ResponseField name="npsByDepartment" type="object">
      <Expandable title="NPS by Department Properties">
        <ResponseField name="npsRecords" type="array">
          Array of department NPS records sorted by total_count descending.
          <Expandable title="Record Properties">
            <ResponseField name="name" type="string">
              Department name.
            </ResponseField>
            <ResponseField name="npsData" type="object">
              <Expandable title="NPS Data Properties">
                <ResponseField name="nps" type="integer">
                  Calculated NPS score (-100 to +100).
                </ResponseField>
                <ResponseField name="total_count" type="integer">
                  Total NPS responses for this department.
                </ResponseField>
                <ResponseField name="details" type="object">
                  <Expandable title="Detail Breakdown">
                    <ResponseField name="promoters" type="object">
                      `{ "count": integer, "percent": number }` — Scores 9–10.
                    </ResponseField>
                    <ResponseField name="detractors" type="object">
                      `{ "count": integer, "percent": number }` — Scores 0–6.
                    </ResponseField>
                    <ResponseField name="passive" type="object">
                      `{ "count": integer, "percent": number }` — Scores 7–8.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="start_date" type="string">
          Start date of the query range (date portion only, e.g. `"2026-01-14"`).
        </ResponseField>
        <ResponseField name="end_date" type="string">
          End date of the query range (date portion only, e.g. `"2026-02-12"`).
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

### Example Response

```json 200
{
  "success": true,
  "message": "Department NPS breakdown retrieved successfully",
  "data": {
    "npsByDepartment": {
      "npsRecords": [
        {
          "name": "Housekeeping",
          "npsData": {
            "nps": 100,
            "total_count": 4,
            "details": {
              "promoters": { "count": 4, "percent": 100 },
              "detractors": { "count": 0, "percent": 0 },
              "passive": { "count": 0, "percent": 0 }
            }
          }
        },
        {
          "name": "Front Desk",
          "npsData": {
            "nps": 67,
            "total_count": 12,
            "details": {
              "promoters": { "count": 10, "percent": 83.3 },
              "detractors": { "count": 2, "percent": 16.7 },
              "passive": { "count": 0, "percent": 0 }
            }
          }
        }
      ],
      "start_date": "2026-01-14",
      "end_date": "2026-02-12"
    }
  }
}
```

### Empty Response

When no NPS questions with departments are found:

```json 200
{
  "success": true,
  "message": "Department NPS breakdown retrieved successfully",
  "data": {
    "npsByDepartment": {
      "npsRecords": [],
      "start_date": "2026-01-14",
      "end_date": "2026-02-12"
    }
  }
}
```

## Error Responses

### Validation Error (400)

```json 400
{
  "success": false,
  "message": "Validation failed",
  "errors": [
    { "field": "startDate", "message": "startDate is required" }
  ]
}
```

### Missing API Key (401)

```json 401
{
  "success": false,
  "message": "API key is required. Provide via X-API-Key header."
}
```
